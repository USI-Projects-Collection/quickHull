


function setup() {
    createCanvas(400, 400); // Create a canvas with width and height
}

function draw() {
    background(220); // Set the background color

    // Draw an ellipse at the center of the canvas
    ellipse(width / 2, height / 2, 50, 50);
}
